"$schema" = "https://conductor.build/schemas/settings.repo.schema.json"

[scripts]
setup = "AGENT_HARNESSES=claude bash scripts/setup/agent/setup.sh"
run_mode = "concurrent"

[scripts.run.agent-setup]
command = "AGENT_HARNESSES=claude bash scripts/setup/agent/verify.sh"
default = true
icon = "test-tube"
