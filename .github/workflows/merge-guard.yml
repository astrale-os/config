name: Merge Guard

on:
  workflow_call:
    inputs:
      dependencies:
        description: 'JSON array of dependency repos'
        required: true
        type: string

jobs:
  merge-guard:
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    steps:
      - uses: astrale-os/config/.github/actions/merge-guard@c02d9486144144dff268910469870345b683e8b3
        with:
          github-token: ${{ github.token }}
          dependencies: ${{ inputs.dependencies }}
